% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lake_ecoregions.R
\name{ok_lake_ecoregion}
\alias{ok_lake_ecoregion}
\title{Look up an Oklahoma lake's EPA Level III ecoregion}
\usage{
ok_lake_ecoregion(lake_name, exact = FALSE, simplify = NULL)
}
\arguments{
\item{lake_name}{Character. One or more lake names to look up.
Partial matching is supported (case-insensitive) unless \code{exact}
is \code{TRUE}.}

\item{exact}{Logical. If \code{TRUE}, requires an exact name match
(case-insensitive). Default \code{FALSE}.}

\item{simplify}{Deprecated. Retained for backward compatibility with
pre-0.1.3 callers; ignored with a deprecation warning. The function
always returns a data frame.}
}
\value{
A data frame of zero or more rows from \code{\link{ok_lake_ecoregions}}
with all columns. An unmatched lookup returns an empty data frame
(with the correct column structure) plus a warning.
}
\description{
Convenience wrapper around \code{\link{ok_lake_ecoregions}} for retrieving the
ecoregion assignment for one or more lakes. Always returns a data
frame so that the return type is stable across single-match and
multi-match results.

To get a bare ecoregion name string for use in \code{\link[=ok_load]{ok_load()}}, use:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{eco <- ok_lake_ecoregion("Arcadia Lake", exact = TRUE)$eco_l3_name
}\if{html}{\out{</div>}}
}
\examples{
# Standard usage: exact match, extract ecoregion name
eco <- ok_lake_ecoregion("Arcadia Lake", exact = TRUE)$eco_l3_name
eco

# Partial match: returns a data frame of all matches
ok_lake_ecoregion("Tenkiller")

# Use in pipeline
eco <- ok_lake_ecoregion("Arcadia Lake", exact = TRUE)$eco_l3_name
if (length(eco) == 1L && !is.na(eco)) {
  ok_load(inflow_m3yr   = 45e6,
          tp_inflow_ugl = 120,
          coefficients  = "oklahoma",
          ecoregion     = eco)
}

}
\seealso{
\code{\link{ok_lake_ecoregions}}, \code{\link[=ok_reservoir]{ok_reservoir()}}
}
